language: python
python:
    - "2.7"

install:
    - python -c "$(curl -fsSL https://raw.githubusercontent.com/platformio/platformio/master/scripts/get-platformio.py)"

script:
    - platformio ci Game_Stable/Shoulder_Dev32/src --board=uno --board=nanoatmega328 --board=megaatmega2560 --board=esp32dev
    - platformio ci Game_Stable/Watch_Mega/src --board=uno --board=nanoatmega328 --board=megaatmega2560 --board=esp32dev
    
notifications:
  email:
    on_success: change
    on_failure: change
